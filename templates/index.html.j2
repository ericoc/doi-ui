<!DOCTYPE html>
<html class="dark" data-bs-theme="dark" lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="author" content="Eric O'Callaghan">
        <meta name="description" content="DOI Search">
        <meta name="format-detection" content="telephone=no">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>DOI Search</title>
        <link rel="shortcut icon" href="{{ url_for('static', filename='favicon.ico') }}" type="image/x-icon">

        <link href="{{ url_for('static', filename='bootstrap-5.3.6-dist/css/bootstrap.min.css') }}" rel="stylesheet">
        <script src="{{ url_for('static', filename='bootstrap-5.3.6-dist/js/bootstrap.bundle.min.js') }}"></script>
        <link href="{{ url_for('static', filename='bootstrap-icons-1.13.1/bootstrap-icons.min.css') }}" rel="stylesheet">
        <style>
            .icon-link-hover { --bs-icon-link-transform: translate3d(0, -.3rem, 0); }
            svg[role="anchor"], svg[role="img"] { color: var(--bs-primary); }
            svg[role="note"] { --bs-icon-link-transform: none; color: var(--bs-secondary); }
            a[target="_blank"]::after { margin-left: 0.25rem; content: url(data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAoAAAAKCAYAAACNMs+9AAAAQElEQVR42qXKwQkAIAxDUUdxtO6/RBQkQZvSi8I/pL4BoGw/XPkh4XigPmsUgh0626AjRsgxHTkUThsG2T/sIlzdTsp52kSS1wAAAABJRU5ErkJggg==); filter: invert(0.5); }
        </style>
    </head>
    <body>
        <header class="container-fluid header">
            <nav class="navbar navbar-expand-md gap-1">
                <a class="btn btn-nav icon-link icon-link-hover text-white text-decoration-none" href="{{ url_for('index') }}" aria-label="DOI-UI" title="DOI-UI">
                    <svg class="bi" aria-hidden="false" role="img">
                        <use xlink:href="{{ url_for('static', filename='bootstrap-icons-1.13.1/bootstrap-icons.svg') }}#house"></use>
                    </svg>
                    DOI-UI
                </a>
            </nav>
        </header>
        <main class="container-fluid">
    {%- with messages = get_flashed_messages(with_categories=False) %}
        {%- if messages %}
            {%- for message in messages %}
                <div class="alert alert-danger m-3 p-3 border border-light-subtle w-75">
                    {{ message }}
                </div>
            {%- endfor %}
        {%- endif %}
    {%- endwith %}
            <form class="mx-3" method="get">
                <label for="doi">
                    <input class="m-3" id="doi" type="text" style="width: 60%;" maxlength="255" name="doi" tabindex="0" {% if doi %}value="{{ doi.doi }}{% else %}placeholder="10.1038/s41586-024-08156-8{% endif %}"><br>
                    <span class="mx-3 p-3">
                        Please enter a
                        <a class="fst-italic" href="https://www.doi.org/" target="_blank" title="Digital Object Identifier (DOI) [https://www.doi.org/]">Digital Object Identifier (DOI)</a>,
                        such as <a class="font-monospace" href="?doi=10.1038/s41586-024-08156-8">10.1038/s41586-024-08156-8</a>.
                    </span>
                </label>
                <p>
                    <input class="btn btn-outline-primary m-3" type="submit" tabindex="1" value="Search">
                    <input class="btn btn-outline-secondary m-3" type="reset" value="Clear">
                </p>
            </form>
    {%- if doi %}
            <div class="container-fluid border-3 border-top border-light-subtle">
                <h1 class="m-3"><a href="{{ doi.url }}" target="_blank" title="{{ doi.title }} <{{ doi.url }}>">{{ doi.title }}</a></h1>
                <div class="row">
                    <h2 class="col font-monospace m-3" title="{{ doi.doi }}">{{ doi.doi }}</h2>
                    <h3 class="col m-3" title="{{ doi.publisher }}">{{ doi.publisher }}</h3>
                    <h3 class="col m-3" title="{{ doi.type }}">{{ doi.type }}</h3>
                </div>
                <table class="table table-bordered table-responsive table-hover table-striped border-light-subtle container-fluid">
                    <thead>
                        <tr class="fw-bold">
                            <td class="bg-black">Date Type</td>
                            <td class="bg-black">When</td>
                            <td class="bg-black">Timestamp</td>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>Created</td>
                            <td>{{ doi.created.strftime("%c") }}</td>
                            <td class="font-monospace">{{ doi.created }}</td>
                        </tr>
                        <tr>
                            <td>Deposited</td>
                            <td>{{ doi.deposited.strftime("%c") }}</td>
                            <td class="font-monospace">{{ doi.deposited }}</td>
                        </tr>
                        <tr>
                            <td>Indexed</td>
                            <td>{{ doi.indexed.strftime("%c") }}</td>
                            <td class="font-monospace">{{ doi.indexed }}</td>
                        </tr>
                        <tr>
                            <td>Issued</td>
                            <td>{{ doi.issued.strftime("%c") }}</td>
                            <td class="font-monospace">{{ doi.issued }}</td>
                        </tr>
                        <tr>
                            <td>Published</td>
                            <td>{{ doi.published.strftime("%c") }}</td>
                            <td class="font-monospace">{{ doi.published }}</td>
                        </tr>
                        <tr>
                            <td>Published Online</td>
                            <td>{{ doi.published_online.strftime("%c") }}</td>
                            <td class="font-monospace">{{ doi.published_online }}</td>
                        </tr>
                        <tr>
                            <td>Published Print</td>
                            <td>{{ doi.published_print.strftime("%c") }}</td>
                            <td class="font-monospace">{{ doi.published_print }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
    {%- endif %}
        </main>
    </body>
</html>
